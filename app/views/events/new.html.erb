<div class="main-body">
  <h2>飲み会作成</h2>
  <%= form_with model: @event, url: group_events_path(@group), method: :post, locul: true do |f| %>
    <%= f.label "飲み会名" %>
    <%= f.text_field :name %>
    <%= f.label "日にち" %>
    <%= f.date_select :date %>
      <div>
        <p>追加メンバー</p>
        <table class="table">
          <% @users.each do |user| %>
            <tr>
              <td><%= attachment_image_tag user, :image, :fill, 60, 60, fallback: 'no-image.png', id: 'member-image' %></td>
              <td><%= user.nickname %></td>
              <td><%= f.check_box :user_ids, { multiple: true, checked: @event.users.find_by(id: user.id).present?,include_hidden: false}, user[:id] %></td>
            </tr>
          <% end %>
        </table>
      </div>
    <%= f.submit "作成" %>
  <% end %>
</div>